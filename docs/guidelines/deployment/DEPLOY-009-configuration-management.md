# DEPLOY-009 Environment Configuration Management

**Applies to:** backend, web, mobile

## Guideline

All environment-specific configuration must be externalized from the codebase. Configuration values must never be hardcoded.
